<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Reservation Alert - AbyRide</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      color: white;
      padding: 40px 30px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .header::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
      animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.5; }
      50% { transform: scale(1.1); opacity: 0.8; }
    }

    .header h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      position: relative;
      z-index: 1;
    }

    .header p {
      font-size: 1.2rem;
      opacity: 0.9;
      position: relative;
      z-index: 1;
    }
        .header img {
      max-width:200px;
      margin-bottom: 20px;
      height:100px;
    }


    .content {
      padding: 30px;
    }

    .alert-section {
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      color: white;
      padding: 25px;
      border-radius: 15px;
      margin-bottom: 30px;
      position: relative;
      overflow: hidden;
    }

    .alert-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, transparent 30%, rgba(255,255,255,0.1) 50%, transparent 70%);
      animation: shimmer 3s ease-in-out infinite;
    }

    @keyframes shimmer {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }

    .alert-section h2 {
      font-size: 1.8rem;
      margin-bottom: 15px;
      position: relative;
      z-index: 1;
    }

    .alert-section p {
      margin-bottom: 10px;
      font-size: 1.1rem;
      position: relative;
      z-index: 1;
    }

    .urgent-badge {
      background: rgba(255, 255, 255, 0.2);
      color: white;
      padding: 8px 16px;
      border-radius: 25px;
      font-weight: 600;
      display: inline-block;
      margin-top: 10px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      position: relative;
      z-index: 1;
    }

    .customer-info, .trip-details {
      margin-bottom: 30px;
    }

    .customer-info h3, .trip-details h3 {
      color: #293751;
      font-size: 1.5rem;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 3px solid transparent;
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      position: relative;
    }

    .customer-info h3::after, .trip-details h3::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 50px;
      height: 3px;
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      border-radius: 2px;
    }

    .info-grid, .details-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 15px;
    }

    .info-item, .detail-card {
      background: linear-gradient(135deg, rgba(71, 178, 228, 0.05) 0%, rgba(41, 55, 81, 0.05) 100%);
      border: 1px solid rgba(71, 178, 228, 0.2);
      padding: 20px;
      border-radius: 12px;
      transition: all 0.3s ease;
    }

    .info-item:hover, .detail-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(71, 178, 228, 0.15);
      border-color: #47B2E4;
    }

    .info-item strong, .detail-card strong {
      display: block;
      color: #293751;
      font-weight: 600;
      margin-bottom: 8px;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .info-item span, .detail-card span {
      color: #47B2E4;
      font-size: 1.1rem;
      font-weight: 500;
    }

    .earnings-highlight {
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      color: white;
      padding: 30px;
      border-radius: 15px;
      text-align: center;
      margin-bottom: 30px;
      position: relative;
      overflow: hidden;
    }

    .earnings-highlight::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
      animation: rotate 6s linear infinite;
    }

    @keyframes rotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .earnings-highlight h3 {
      font-size: 1.5rem;
      margin-bottom: 15px;
      position: relative;
      z-index: 1;
    }

    .earnings-amount {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      position: relative;
      z-index: 1;
    }

    .earnings-highlight p {
      font-size: 1.1rem;
      opacity: 0.9;
      position: relative;
      z-index: 1;
    }


    @media (max-width: 600px) {
      .container {
        margin: 10px;
        border-radius: 15px;
      }

      .header {
        padding: 30px 20px;
      }
    
      .header h1 {
        font-size: 2rem;
      }

      .content {
        padding: 20px;
      }

      .info-grid, .details-grid {
        grid-template-columns: 1fr;
      }

      .earnings-amount {
        font-size: 2.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
  <div class="header">
      <img src="https://aspirebridgerwanda.org/abyride-logo.png" alt="Abyride Logo" />
      <h1>🚗 Abyride Application Received</h1>
      <p>You have recieved new reservation request</p>
    </div>

    <div class="content">
      <div class="alert-section">
        <h2>New Ride Request!</h2>
        <p>You have received a new reservation request.</p>
      </div>

      <div class="customer-info">
        <h3>👤 Customer Information</h3>
        <div class="info-grid">
          <div class="info-item">
            <strong>Customer Name</strong>
            <span>{{firstName}} {{lastName}}</span>
          </div>
          <div class="info-item">
            <strong>Phone Number</strong>
            <span>{{customerPhone}}</span>
          </div>
          <div class="info-item">
            <strong>Email</strong>
            <span>{{customerEmail}}</span>
          </div>
          <div class="info-item">
            <strong>Passengers</strong>
            <span>{{numberOfPassengers}}</span>
          </div>
        </div>
      </div>

      <div class="trip-details">
        <h3>🗺️ Trip Details</h3>
        <div class="details-grid">
          <div class="detail-card">
            <strong>Pickup Location</strong>
            <span>{{pickup}}</span>
          </div>
          <div class="detail-card">
            <strong>Dropoff Location</strong>
            <span>{{dropoff}}</span>
          </div>
          <div class="detail-card">
            <strong>Scheduled Time</strong>
            <span>{{scheduledDateTime}}</span>
          </div>
          <div class="detail-card">
            <strong>Distance</strong>
            <span>{{distance}}</span>
          </div>
          <div class="detail-card">
            <strong>Duration</strong>
            <span>{{duration}}</span>
          </div>
          <div class="detail-card">
            <strong>Reservation Number</strong>
            <span>{{reservationNumber}}</span>
          </div>
        </div>
      </div>

      <div class="earnings-highlight">
        <h3>💰 Trip Earnings</h3>
        <div class="earnings-amount">{{price}}</div>
        <p>This is your estimated earnings for this trip</p>
      </div>
    </div>

  
  </div>
</body>
</html>
