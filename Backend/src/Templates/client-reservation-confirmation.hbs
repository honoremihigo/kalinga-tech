<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Reservation Confirmation</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #47B2E4 0%, #293751 100%);
      color: white;
      padding: 40px 30px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

.header img {
      max-width:200px;
      margin-bottom: 20px;
      height:100px;
    }
    .header::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
      animation: pulse 4s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.5; }
      50% { transform: scale(1.1); opacity: 0.8; }
    }

    .header h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
      position: relative;
      z-index: 1;
    }

    .header p {
      font-size: 1.2rem;
      opacity: 0.9;
      position: relative;
      z-index: 1;
    }

    .content {
      padding: 30px;
    }

    .assignment-section h2 {
      font-size: 1.8rem;
      color: #293751;
      margin-bottom: 10px;
    }

    .assignment-section p {
      font-size: 1.1rem;
      margin-bottom: 10px;
      color: #333;
    }

    .details-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 15px;
    }

    .detail-card {
      background: #f4f8fc;
      border: 1px solid #d3e9f8;
      padding: 20px;
      border-radius: 12px;
    }

    .detail-card strong {
      display: block;
      color: #293751;
      font-weight: 600;
      margin-bottom: 8px;
      font-size: 0.9rem;
      text-transform: uppercase;
    }

    .detail-card span {
      color: #47B2E4;
      font-size: 1.1rem;
      font-weight: 500;
    }

    .footer {
      background: #f0f4f9;
      text-align: center;
      padding: 30px;
      margin-top: 30px;
      border-top: 1px solid #d0e2f2;
    }

    .footer p {
      color: #333;
      font-size: 0.95rem;
      margin: 5px 0;
    
  }
     .footer a {
      color: #47B2E4;
      text-decoration: none;
      font-weight: 500;
    }

    @media (max-width: 600px) {
      .container {
        margin: 10px;
        border-radius: 15px;
      }

      .header {
        padding: 30px 20px;
      }

      .header h1 {
        font-size: 2rem;
      }

      .content {
        padding: 20px;
      }

      .details-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
         <img src="https://aspirebridgerwanda.org/abyride-logo.png" alt="Abyride Logo" />
     
      <h1>Reservation Confirmed</h1>
      <p>Hi {{firstName}}, your AbyRide trip is confirmed!</p>
    </div>

    <div class="content">
      <div class="assignment-section">
        <h2>Your Trip Details</h2>
        <p>Here's a summary of your upcoming ride:</p>
      </div>

      <div class="details-grid">
        <div class="detail-card"><strong>Reservation Number</strong><span>{{ReservationNumber}}</span></div>
        <div class="detail-card"><strong>Pickup Location</strong><span>{{pickup}}</span></div>
        <div class="detail-card"><strong>Dropoff Location</strong><span>{{dropoff}}</span></div>
        <div class="detail-card"><strong>Scheduled Date</strong><span>{{scheduledDateTime}}</span></div>
       <div class="detail-card"><strong>Distance</strong><span>{{distance}}</span></div>
        <div class="detail-card"><strong>Duration</strong><span>{{duration}}</span></div>
     </div>
        <div class="detail-card" style="margin-top: 10px;"><strong>Total Price</strong><span>{{price}}</span></div>
     

      <div class="assignment-section" style="margin-top: 30px;">
        <h2>Your Driver</h2>
        <p>Below are the details of your assigned driver:</p>
      </div>

      <div class="details-grid">
        <div class="detail-card"><strong>Driver Name</strong><span>{{driverName}}</span></div>
        <div class="detail-card"><strong>Driver Phone</strong><span>{{driverPhone}}</span></div>
        <div class="detail-card"><strong>Driver Rating</strong><span>{{driverRating}}</span></div>
      </div>

      <div class="assignment-section" style="margin-top: 30px;">
        <h2>Vehicle Information</h2>
      </div>

      <div class="details-grid">
        <div class="detail-card"><strong>Make</strong><span>{{vehicleMake}}</span></div>
        <div class="detail-card"><strong>Model</strong><span>{{vehicleModel}}</span></div>
        <div class="detail-card"><strong>Year</strong><span>{{vehicleYear}}</span></div>
        <div class="detail-card"><strong>Plate Number</strong><span>{{vehiclePlate}}</span></div>
      </div>

      <div style="margin-top: 30px; text-align: center;">
        <!-- Payment link will only show if payment method is credit card -->
        {{#if showPaymentLink}}
          <a href="{{paymentUrl}}" style="background:#47B2E4;color:white;padding:12px 24px;border-radius:8px;text-decoration:none;font-weight:bold;">Pay Now</a>
          <br/><br/>
        {{/if}}
        
        <a href="{{cancelUrl}}" style="color:#dc2626;font-size:0.95rem;text-decoration:underline;">Cancel Reservation</a>
      </div>
    </div>

  
    <div class="footer">
            <h3>Need Assistance?</h3>
            <p>Our support team is available 24/7 to help with any questions</p>
            <p><a href="mailto:support@abyride.com">support@abyride.com</a> | <a href="tel:+1234567890">+1 (234) 567-890</a></p>
            <p style="margin-top: 30px; font-size: 14px; opacity: 0.9;">
                Thank you for choosing Abyride LLC - Your trusted transportation partner<br>
                <em>Excellence in Motion, Service in Every Mile</em>
            </p>
        </div>
  </div>
</body>
</html>